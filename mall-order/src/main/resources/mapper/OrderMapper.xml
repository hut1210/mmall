<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mmall.order.mapper.OrderMapper">

    <select id="getOrderList" resultType="com.mmall.order.dto.Order">
        select * from o_order
    </select>
    <select id="">
        <foreach collection="list">

        </foreach>
    </select>
</mapper>